# Table 1 Reference

This folder keeps the original Table 1 screenshot/reference material from the
early reconstruction work.

The old standalone React app was removed. The deployed implementation now lives
in `../site/`, and the data pipeline lives in
`../Database/retrosheet_pipeline.ipynb`.

For new work, update the notebook first, then update the table component in
`../site/src/tables/`.
